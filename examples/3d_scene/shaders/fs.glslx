#version 330 core
out vec4 FragColor;
  
in vec3 Color;
in vec2 TexCoord;

uniform sampler2D texture0;

void main()
{
  vec4 texColor = texture(texture0, TexCoord);
  FragColor = texColor * vec4(Color, 1.0f);
}
